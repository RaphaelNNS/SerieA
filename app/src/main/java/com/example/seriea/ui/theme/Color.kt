package com.example.seriea.ui.theme

import androidx.compose.ui.graphics.Color

// ============ LIGHT MODE ============

// Primary (azul do app)
val BluePrimary = Color(0xFF2563EB)
val BluePrimaryDark = Color(0xFF1D4ED8)
val BlueLightBackground = Color(0xFFEFF6FF)

// Backgrounds
val Background = Color(0xFFF9FAFB)        // Fundo principal
val Surface = Color(0xFFFFFFFF)           // Cards, navigation bar

// Text
val TextPrimary = Color(0xFF111827)       // Texto principal
val TextSecondary = Color(0xFF6B7280)     // Texto secundário
val TextTertiary = Color(0xFF9CA3AF)      // Legendas pequenas

// Borders / Dividers
val Border = Color(0xFFE5E7EB)            // Bordas e divisores

// Icons / States
val StarActive = Color(0xFFFACC15)        // Estrela do time favorito
val IconInactive = Color(0xFF9CA3AF)      // Ícones inativos
val IconActive = Color(0xFF2563EB)        // Ícones ativos (navigation)
val Gray600 = Color(0xFF4B5563)           // Ícones navigation inativos

// Match Status
val LiveRed = Color(0xFFDC2626)           // Badge "AO VIVO"
val LiveRedBg = Color(0xFFFEE2E2)         // Fundo badge "AO VIVO"
val ScheduledBlueBg = Color(0xFFEFF6FF)   // Fundo badge agendado
val ScheduledBlue = Color(0xFF2563EB)     // Texto badge agendado
val FinishedGrayBg = Color(0xFFF3F4F6)    // Fundo badge encerrado
val FinishedGray = Color(0xFF6B7280)      // Texto badge encerrado

// Result Colors
val WinGreen = Color(0xFF22C55E)
val WinGreenBg = Color(0xFFF0FDF4)
val DrawYellow = Color(0xFFFACC15)
val DrawYellowBg = Color(0xFFFEF9C3)
val LossRed = Color(0xFFEF4444)
val LossRedBg = Color(0xFFFEE2E2)

// ============ DARK MODE ============

// Primary (mantém o azul)
val BluePrimaryDarker = Color(0xFF2563EB)

// Backgrounds
val DarkBackground = Color(0xFF0F172A)    // Fundo geral (azul escuro quase preto)
val DarkSurface = Color(0xFF1E293B)       // Cards, navigation bar

// Text
val DarkTextPrimary = Color(0xFFF9FAFB)   // Texto principal (branco suave)
val DarkTextSecondary = Color(0xFFD1D5DB) // Texto secundário
val DarkTextTertiary = Color(0xFF9CA3AF)  // Legendas pequenas

// Borders / Dividers
val DarkBorder = Color(0xFF334155)        // Bordas e divisores

// Icons / States
val StarActiveDark = Color(0xFFFACC15)    // Estrela (mesma cor)
val IconInactiveDark = Color(0xFF64748B)  // Ícones inativos